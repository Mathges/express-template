//ici un simple require du server fonctionne, cela va importer notre module server.js
require('./project_modules/server')

//il sera normal d'avoir un 'Cannot GET /' sur la page web 
// tant que 'http://localhost:3000' apparait dans le terminal ça veut dire que le server fonctionne

// à noter: app.js est ici un équivalent à index.js